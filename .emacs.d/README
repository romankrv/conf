If you want to start this config that
you must make pre-install steps:

1) Install  el-get --> el-get allows you to install and manage elisp code for Emacs.
   How to Install it?

   ;; So the idea is that you copy/paste this code into your *scratch* buffer,
   ;; hit C-j, and you have a working el-get.
   (url-retrieve
    "https://github.com/dimitri/el-get/raw/master/el-get-install.el"
    (lambda (s)
     (end-of-buffer)
     (eval-print-last-sexp)))

    You have to type C-j with the cursor at the end of the last line, 
    but still on the line. C-j runs the command eval-print-last-sexp,
    so it will evaluate the code you’re looking at, and that will git 
    clone el-get at the right place.

2) pass